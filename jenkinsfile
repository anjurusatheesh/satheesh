pipeline {
    agent any

    stages {
        stage('Create a File') { 
            steps {
                // Create a file directly in the home directory
                sh 'touch ~/satheesh.txt'
            }
        }
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
